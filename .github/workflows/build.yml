name: Build Android APK
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install --force

      - name: Build APK
        run: |
          echo "Starting Build..."
          # سنضيف خطوات Capacitor هنا بمجرد التأكد من ملف package.json

