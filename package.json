{
  "name": "flamenco-admin",
  "version": "1.0.0",
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/cli": "latest",
    "@capacitor/core": "latest"
  },
  "scripts": {
    "build": "mkdir -p www && cp *.html www/"
  }
}

